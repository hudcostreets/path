# scrns GIF generation for homepage

## TODO
- Add `pin=` URL param to RidesPlot that sets initial pinned station on load
- Create scrns config with:
  - GIF 1: plot1 by-station, each station highlighted in order (sorted by last y-value)
  - GIF 2: plot2 monthly, each year highlighted in order
  - Both at same viewport/scroll position showing both plots
  - Extra beat (frame pause) on first and last traces
- Also capture static screenshots for README and og:image
- Test parallel vs sequential capture wall time
